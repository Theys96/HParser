language: haskell
ghc:
  - "8.2"

# command to build
install:
  - make install
  - make build
  - make clean

# command to run tests
script:
  - HGrammar/HGrammar grammar1 grammar1 S
  - HGrammar/HGrammar grammar2 grammar2 S
  - HGrammar/HGrammar grammar3 grammar3 S
  - HGrammar/HGrammar grammar5 grammar5 S
  - ./test
